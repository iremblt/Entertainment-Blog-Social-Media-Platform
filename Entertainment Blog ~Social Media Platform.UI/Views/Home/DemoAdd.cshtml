@model Demo
@{
    ViewData["Title"] = "Add Demo";
}
<div class="container m-4">
    <div class="card mx-auto" style="width: 500px;">
        <div class="card-header">
            Add Demo Form
        </div>
        <div class="card-body">
            <form asp-action="demoAdd" method="POST">
                <div asp-validation-summary="All"></div>
                <div class="form-group">
                    <label asp-for="Title">Title: </label>
                    <input asp-for="Title" class="form-control">
                </div>
                <div ass="col-md-10">
                    <textarea style="border-color:grey;" asp-for="ShortDes" id="ShortDes" name="ShortDes" cols="70" placeholder="Short Description"></textarea>
                </div>
                <div ass="col-md-10">
                    <textarea asp-for="Descrip" id="Descrip" class="ckeditor" name="Descrip" rows="200"></textarea>
                    @*@Html.Raw(HttpUtility.HtmlDecode(Model.Descrip)).ToHtmlString();*@
                </div>
                @*<div class="form-group">
            <label asp-for="Descrip">Description: </label>
            @Html.TextAreaFor(model => model.Descrip, new { @id = "editor1" })
            <script type="text/javascript">
                var editor = CKEDITOR.instances['editor1'];
                if (editor) { editor.destroy(true); }
                CKEDITOR.replace('editor1', {
                    enterMode: CKEDITOR.ENTER_BR,
                });
            </script>
        </div>
        <div class="form-group">
            <label asp-for="ShortDes">Summary: </label>
            @Html.TextAreaFor(model => model.ShortDes, new { @id = "editor1" })
            <script type="text/javascript">
                var editor = CKEDITOR.instances['editor1'];
                if (editor) { editor.destroy(true); }
                CKEDITOR.replace('editor1', {
                    enterMode: CKEDITOR.ENTER_BR,
                });
            </script>
        </div>*@
                <div class="form-group">
                    <label asp-for="DateTime">Date Time: </label>
                    <input asp-for="DateTime" class="form-control">
                </div>
                <div class="form-group">
                    <label asp-for="Image">Image: </label>
                    <input asp-for="Image" class="form-control">
                </div>
                <div style="padding-top:10px">
                    <button class="btn btn-outline-primary" type="submit" value="submit">Submit</button>
                </div>
            </form>
         </div>
    </div>
</div>